<UserControl x:Class="Clash_WPF.Views.ProxyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:Clash_WPF.Converters">

    <UserControl.Resources>
        <conv:DelayToColorConverter x:Key="DelayColorConv"/>
        <conv:DelayToStringConverter x:Key="DelayConv"/>
        <conv:BoolToVisibilityConverter x:Key="BoolVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="4,0,4,12">
            <TextBlock Text="代理" Style="{StaticResource PageTitle}" DockPanel.Dock="Left"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Button Content="测速全部" Style="{StaticResource SecondaryButton}"
                        Command="{Binding TestAllDelayCommand}" Margin="0,0,8,0"/>
                <Button Content="刷新" Style="{StaticResource PrimaryButton}"
                        Command="{Binding RefreshCommand}"/>
            </StackPanel>
        </DockPanel>

        <!-- Status Message -->
        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" Margin="4,0,4,8"
                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>

        <!-- Group Tabs -->
        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="4,0,4,12">
            <ItemsControl ItemsSource="{Binding Groups}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="Tab" CornerRadius="6" Padding="12,6" Margin="0,0,6,0" Cursor="Hand"
                                Background="{StaticResource CardBgBrush}" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}"
                                MouseLeftButtonDown="GroupTab_Click" Tag="{Binding}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13"/>
                                <TextBlock Text="{Binding Type, StringFormat=' ({0})'}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsGroupSelected}" Value="True">
                                <Setter TargetName="Tab" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="Tab" Property="Background" Value="#2a2a4a"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Proxy Nodes Grid -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding SelectedGroup.Nodes}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="NodeCard" Width="185" Margin="4,4" Padding="12,10" CornerRadius="8"
                                Background="{StaticResource CardBgBrush}" Cursor="Hand"
                                BorderThickness="1" BorderBrush="Transparent"
                                MouseLeftButtonDown="NodeCard_Click" Tag="{Binding}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Node Name -->
                                <TextBlock Grid.Row="0" Text="{Binding Name}" FontSize="13" FontWeight="Medium"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           TextTrimming="CharacterEllipsis" ToolTip="{Binding Name}"/>

                                <!-- Type + Delay -->
                                <DockPanel Grid.Row="1" Margin="0,4,0,0">
                                    <TextBlock Text="{Binding Type}" FontSize="11"
                                               Foreground="{StaticResource TextSecondaryBrush}" DockPanel.Dock="Left"/>
                                    <TextBlock Text="{Binding Delay, Converter={StaticResource DelayConv}}"
                                               FontSize="11" HorizontalAlignment="Right"
                                               Foreground="{Binding Delay, Converter={StaticResource DelayColorConv}}"/>
                                </DockPanel>

                                <!-- Selected indicator -->
                                <Border Grid.Row="2" Height="3" CornerRadius="2" Margin="0,6,0,0"
                                        Visibility="{Binding IsSelected, Converter={StaticResource BoolVis}}"
                                        Background="{StaticResource PrimaryBrush}"/>
                            </Grid>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                <Setter TargetName="NodeCard" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="NodeCard" Property="BorderThickness" Value="1"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
